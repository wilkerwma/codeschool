{% extends "viewpack/crud/form.jinja2" %}

{% block content_title %}{{ verbose_name|title }}: {{ detail_object.title }}{% endblock %}

{% block form_actions %}
    <button type="submit" form="{{ form_id }}" name="action" value="delete" class="form__delete-button">{{ _('Delete') }}</button>
    <button type="submit" form="{{ form_id }}" name="action" value="copy" class="form__copy-button">{{ _('Copy') }}</button>
    {{ super() }}
{% endblock %}